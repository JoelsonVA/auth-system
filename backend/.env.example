# Database connection
DB_HOST=localhost
DB_PORT=3306
DB_USER=freelancehub_user
# Use a non-sensitive placeholder in the example
DB_PASSWORD=changeme
DB_NAME=freelancehub

# JWT secret (change to a strong random value in production)
JWT_SECRET=change_this_secret

# Application port
PORT=3000
